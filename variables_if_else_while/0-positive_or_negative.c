#include <stdio.h>
#include <time.h>
/* headers goes there */ 

/* function main goes there */
int main(void)
{
   int n;

   srand(time(0));
   n = rand() - RAND_MAX / 2;
   /* Code goes there */
   return (0);
}
