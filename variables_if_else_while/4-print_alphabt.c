#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)i
{
	char letter;
	char characters = ("q,e";)

	for (letter = 'a'; letter <= 'z'; letter++)
		putchar(letter,characters);
	putchar('\n');

	return (0);
}
