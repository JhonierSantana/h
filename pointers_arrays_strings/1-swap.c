/*
 * File: 1-swap.c
 * Auth: Jhonier Santana
 */

#include "main.h"

/**
 * swap_int - that swaps the values of two integers.
 *
 * @a: pointer
 * @b: pointer
 */

void swap_int(int *a, int *b)
{
 printf("\n *a is %d", *a);
 printf("\n +b is %d", *b);
}
