/**
 * File: 2-mul.c
 * Auth: Jhonier Santana
 */
#include "main.h"
/**
 * mul - checks for checks for a digit (0 through 9).
 * @a: variable
 * @b: variable
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
