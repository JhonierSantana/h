#include<stdio.h>

/**
 * main -Prints the alphabet in lowercase.
 *
 * Return always 0
 */
int main(void)
{
	char letter;

	for(letter = 'a'; letter <= 'z'; letter++)
		if(letter = 'A'; letter <= 'Z'; letter++)
		puthcar(letter);

	putchar(\'n');
	
	return (0);

}
